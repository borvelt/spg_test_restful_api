/*
 * here is energyConsumption filters.
 * to have better developer experience we should use other libraries like `immer` to make sure we are returning new object
 * with immer we don't have to use a nested `...` operator
 * another solution is using other libraries like Ramda.*/
export {};
